# Example Environment Variables
# WARNING: Never put actual credentials in files!
# In Replit, use the Secrets tab to set these values securely

# Database Configuration (stored in Replit Secrets)
DATABASE_URL=postgresql://user:password@host/database?sslmode=require
PROD_DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# AWS S3 Configuration (stored in Replit Secrets)
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
AWS_REGION=ca-central-1
S3_BUCKET_NAME=your-bucket-name

# API Keys (stored in Replit Secrets)
OPENAI_API_KEY=sk-...
PERPLEXITY_API_KEY=pplx-...
ANTHROPIC_API_KEY=sk-ant-...

# JWT Configuration (stored in Replit Secrets)
JWT_SECRET=your-jwt-secret-key