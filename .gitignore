node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment files with sensitive data
.env
.env.production
.env.local
.env.*.local

# Database backups (may contain sensitive data)
database-backups/